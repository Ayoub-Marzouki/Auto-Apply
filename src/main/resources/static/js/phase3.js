// Phase 3: Intent textarea hooks (future: send to LLM)
(function(){
  const taId='intentText';
  document.addEventListener('DOMContentLoaded',()=>{
    const ta=document.getElementById(taId);
    if(!ta) return;
    // future: add live char count or validation
  });
})();
